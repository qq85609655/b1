package ${package}.repository;

import ${package}.entity.${className}Entity;
import org.springframework.data.repository.PagingAndSortingRepository;

/**
 * ${comments}
 *
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${className}Repository extends PagingAndSortingRepository<${className}Entity, String> {
}